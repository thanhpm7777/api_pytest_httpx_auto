[pytest]
addopts = -q -ra --strict-markers --alluredir=./reports/allure
markers =
    smoke: Fast checks on key endpoints
    regression: Full business flows
    contract: Pact consumer/provider tests
    schemathesis: Schema‑based property tests
    perf: Locust smoke (trigger only on demand)